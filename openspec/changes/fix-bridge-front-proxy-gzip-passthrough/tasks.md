## 1. Fix

- [x] 1.1 Disable `auto_decompress` on the bridge front proxy upstream client session

## 2. Tests

- [x] 2.1 Add regression test proving gzip-encoded worker responses pass through the front proxy verbatim
