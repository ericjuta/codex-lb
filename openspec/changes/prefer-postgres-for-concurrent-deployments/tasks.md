## 1. Specs

- [ ] 1.1 Add a `database-backends` requirement that keeps SQLite as the default backend while recommending PostgreSQL for higher-concurrency deployments.
- [ ] 1.2 Add a `deployment-installation` requirement for a documented PostgreSQL-backed Docker performance profile.
- [ ] 1.3 Validate the OpenSpec artifacts.

## 2. Documentation and examples

- [ ] 2.1 Update `README.md` configuration and Docker guidance to call out PostgreSQL as the recommended backend for higher-concurrency deployments.
- [ ] 2.2 Update `.env.example` and/or `docker-compose.yml` examples so operators have a supported PostgreSQL-backed Docker path without changing the standard service ports.
